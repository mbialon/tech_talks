<%= form_for @attendance do |att| %>
	<div>
		<%= att.hidden_field :talk_id, :value => @talk.id  %>
	</div>
	<div>
		<%= att.hidden_field :user_id, :value => current_user.id  %>
	</div>
	<div>
		<%= att.submit "Zapisz sie" %>
	</div>
<% end %>
